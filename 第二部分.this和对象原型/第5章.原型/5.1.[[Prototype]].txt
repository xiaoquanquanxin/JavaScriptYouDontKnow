1.[[Prototype]]是一个内置对象，是对于其他对象的引用。
2.for...in查询、in操作符和查询[[]]链类似。
3.用户定义的大多数对象的[[]]最终都会指向Object.prototype。
4.赋值操作：
如果自身和原型链上都找不到某个属性，则会添加到自身。
如果属性在原型链上的某一层时，有3种情况：
    1.属性是普通数据属性、可写，则给自身添加属性。
    2.属性是不可写的，忽略操作。'use strict'下报错。
    3.属性是一个setter，调用setter，只有getter，忽略。不添加属性。
5.